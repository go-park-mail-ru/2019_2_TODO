syntax = "proto3";

package session;

message Session {
    string username = 1;
    string avatar = 2;
    bool admin = 3;
}

message Nothing {
  bool dummy = 1;
}

service AuthChecker {
    rpc handleSignInGet(Nothing) returns (Session) {}
}